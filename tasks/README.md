# Tasks

Use a task file only when work will span multiple meaningful steps or sessions.

- Start from `tasks/TEMPLATE.md`.
- Keep active task files short and current.
- Use filename numbering as the canonical task ID.
- Put implementation details in code and tests, not in task docs.
- Archive completed tasks under `tasks/completed/`.
